# Full-stack fully-typed API

This repo contains an example of typed requests and responses in your server and client code
with `fastify` and `react-query`, using `json-schema-to-ts`, `fastify-swagger`, and `orval`.

